# Context packs

Lightweight **path hints** per agent (`*.json`). Pair with **`factory/agent-registry.json`** `context_pack` fields.

Use when composing Cursor prompts: `@` only these paths + the role file to reduce noise.
