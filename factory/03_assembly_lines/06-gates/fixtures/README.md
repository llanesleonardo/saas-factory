# Validation fixtures (station **06-gates**)

Golden / negative samples for factory validators (`*-fixtures.ts` under **`../validation/`**). **Canonical location:** `factory/03_assembly_lines/06-gates/fixtures/`.

## Symlink (optional)

From repo `factory/`:

```bash
ln -sf "03_assembly_lines/06-gates/fixtures" factory/fixtures
```
