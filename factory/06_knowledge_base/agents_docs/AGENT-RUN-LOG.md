# AGENT RUN LOG (MANUAL)

Append a row after a **Cursor session** where you used `@agents/<role>-agent.md` so the team has a lightweight audit trail. **Nothing writes here automatically** unless you add tooling later.

## Format (copy block per session)

```text
| YYYY-MM-DD | agent-file | task id | outcome | PR / issue | notes |
```

## Log

| Date | Agent | Task id | Outcome | PR / issue | Notes |
|------|---------|---------|---------|------------|-------|
| _example_ | `dev-agent.md` | `PLU-001` | done | #12 | Merged after QA green |

---

_Add new rows above the example line or replace the example when you start logging._
