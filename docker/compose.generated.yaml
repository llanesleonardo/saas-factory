# Generated by npm run app:scaffold — app "todoapp" (configs/app.blueprint.json)
# Do not edit by hand; re-run scaffold after blueprint changes.
#
# Infra (Redis / Postgres): docker compose -f docker/compose.yaml --profile infra up -d

services:
  redis:
    profiles: [infra]
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

