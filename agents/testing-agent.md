# MOVED — merged into Quality Agent

**`agents/testing-agent.md`** is merged into **`agents/quality-agent.md`**.

Use **`@agents/quality-agent.md`** for **local/CI test environments**, fixtures, mocks, seeds **and** running build/tests with **`{ "status", "errors" }`** output.
