{
  "name": "todo-instance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c auto \"npm run dev -w apps/todo-instance/frontend\" \"npm run dev -w apps/todo-instance/backend\"",
    "build": "npm run build -w apps/todo-instance/frontend && npm run build -w apps/todo-instance/backend",
    "test": "npm run test -w apps/todo-instance/frontend",
    "lint": "npm run lint -w apps/todo-instance/frontend",
    "format": "npm run format -w apps/todo-instance/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
